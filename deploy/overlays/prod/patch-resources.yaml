apiVersion: apps/v1
kind: Deployment
metadata:
  name: sample-go-service
spec:
  template:
    spec:
      containers:
        - name: sample-go-service
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 512Mi
