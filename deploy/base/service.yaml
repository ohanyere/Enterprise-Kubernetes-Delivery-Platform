apiVersion: v1
kind: Service
metadata:
  name: sample-go-service
  labels:
    app.kubernetes.io/name: sample-go-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: sample-go-service
  ports:
    - name: http
      port: 80
      targetPort: http
