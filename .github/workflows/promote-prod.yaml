name: Promote Production

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  promote-prod:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install yq
        run: |
          sudo wget -qO /usr/local/bin/yq https://github.com/mikefarah/yq/releases/download/v4.44.3/yq_linux_amd64
          sudo chmod +x /usr/local/bin/yq
          yq --version

      - name: Read promoted image
        run: |
          echo "IMAGE_NAME=$(yq '.images[] | select(.name == "docker.io/kuberpull/sample-go-service") | .newName' deploy/overlays/staging/kustomization.yaml)" >> "$GITHUB_ENV"
          echo "IMAGE_TAG=$(yq '.images[] | select(.name == "docker.io/kuberpull/sample-go-service") | .newTag' deploy/overlays/staging/kustomization.yaml)" >> "$GITHUB_ENV"

      - name: Verify staging image is immutable
        run: |
          test -n "${IMAGE_NAME}"
          test -n "${IMAGE_TAG}"
          test "${IMAGE_TAG}" != "latest"
          case "${IMAGE_TAG}" in
            sha-*) ;;
            *)
              echo "Staging image tag must be an immutable sha-* tag, got: ${IMAGE_TAG}"
              exit 1
              ;;
          esac

      - name: Update prod overlay image
        run: |
          yq -i '
          (.images[] | select(.name == "docker.io/kuberpull/sample-go-service") | .newName) = strenv(IMAGE_NAME) |
          (.images[] | select(.name == "docker.io/kuberpull/sample-go-service") | .newTag) = strenv(IMAGE_TAG)
          ' deploy/overlays/prod/kustomization.yaml

      - name: Render prod overlay
        run: kubectl kustomize deploy/overlays/prod

      - name: Commit and push production promotion
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          if git diff --quiet -- deploy/overlays/prod/kustomization.yaml; then
            echo "No production promotion changes to commit."
            exit 0
          fi

          git add deploy/overlays/prod/kustomization.yaml
          git commit -m "chore: promote production to ${IMAGE_TAG} [skip ci]"
          git pull --rebase origin main
          git push origin HEAD:main
